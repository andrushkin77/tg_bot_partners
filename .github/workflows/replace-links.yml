name: Replace URLs Everywhere

on:
  workflow_dispatch:
    inputs:
      old_url:
        description: "Старая ссылка"
        required: true
      new_url:
        description: "Новая ссылка"
        required: true

jobs:
  replace:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Replace URLs (без .github/workflows)
        run: |
          echo "Заменяем: ${{ github.event.inputs.old_url }} → ${{ github.event.inputs.new_url }}"
          find . \
            -type f \
            \( -name "*.html" -o -name "*.js" -o -name "*.ts" -o -name "*.md" \) \
            -not -path "./.github/workflows/*" \
            -exec sed -i "s|${{ github.event.inputs.old_url }}|${{ github.event.inputs.new_url }}|g" {} +

      - name: Commit and push changes
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add -u
          git diff --quiet && echo "Нет изменений — пуш не нужен." || (
            git commit -m "🔁 Заменил ссылки: ${{ github.event.inputs.old_url }} → ${{ github.event.inputs.new_url }}"
            git push https://x-access-token:${TOKEN}@github.com/${{ github.repository }} HEAD:${{ github.ref_name }}
          )
